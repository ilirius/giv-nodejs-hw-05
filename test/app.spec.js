const chai = require('chai');
const should = chai.should();
const chaiHttp = require('chai-http');
chai.use(chaiHttp);

describe('Тестирование авторизации метод POST', () => {
    it('Просто тест', (done) => {
        done();
    });
});