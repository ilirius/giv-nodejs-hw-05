/* eslint-disable no-unused-vars */
/* eslint-disable no-undef */
import chai from 'chai';
import chaiHttp from 'chai-http';
// import Mocha, {
//   describe
// } from 'mocha';

const should = chai.should();
chai.use(chaiHttp);

describe('Тестирование авторизации метод POST', () => {
  it('Вход через логин', (done) => {
    done();
  });

  it('Вход через Token', (done) => {
    done();
  });
});
